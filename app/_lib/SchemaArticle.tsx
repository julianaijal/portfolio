const SchemaArticle = () => {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{
        __html: `[{
          "@context":"https://schema.org",
          
       }]`,
      }}
    />
  );
};

export default SchemaArticle;
